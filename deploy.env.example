# Copy this file to "deploy.env" and fill in your real server details.
# deploy.env is gitignored so your password never gets committed.

SERVER_IP=2.25.152.153
SERVER_PORT=22
SERVER_USER=root
SERVER_PASSWORD=your_server_password_here
REMOTE_DIR=/opt/leadgen
